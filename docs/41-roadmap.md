# 41 - ロードマップ (Roadmap)

> ADRS Demo 01 を **20〜30 回のやり取り** で完成させるための進行計画。
> 1 ターン = 1 回のやり取り（仕様確定 or 実装単位）。**実装は仕様合意の後** に行う。

---

## 全体像（5 フェーズ）

| フェーズ | 内容 | ターン目安 |
|---|---|---|
| 0. 実装前（仕様確定） | スコープ・データ・イベント・台本・技術方針 | 5〜6 |
| 1. 基礎実装 | マップ表示・移動・カメラ | 5〜6 |
| 2. RPG 機能（最大の山） | イベントエンジン・会話・宝箱・ワープ・戦闘 | 8〜10 |
| 3. AI サポート | NPC セリフ提案（1 機能） | 2〜3 |
| 4. デモ完成 | 導線結合・テストプレイ・演出最小強化 | 3〜4 |
| 合計 | | **23〜29** |

> **成功の分岐点はフェーズ 2 のイベントエンジン。**
> 「汎用コマンド列」として綺麗に作れれば、会話/宝箱/ワープ/戦闘が芋づる式に完成する。
> 個別実装すると 30 ターンでは終わらない。

---

## フェーズ 0: 実装前（仕様確定）— 5〜6 ターン

| T | やること | 成果物 |
|---|---|---|
| T1 | スコープ確定（本決定方針の反映） | `01-demo01-scope.md` ✅ |
| T2 | 未決定事項への回答・決定 | 決定ログ |
| T3 | データモデル確定 | `11-data-model.md` ✅ |
| T4 | イベントコマンド最小セット確定 | `21-event-system-spec.md` ✅ |
| T5 | デモ用ミニシナリオ台本 | `40-demo01-scenario.md` ✅ |
| **T5.5** | **Codex 技術レビュー反映ゲート**（下記） | `docs/reviews/CODEX_REVIEW_001.md` ✅ / 仕様修正 ✅ |
| T6 | ジオラマ表現方針 | `24-diorama-rendering.md` ✅ |
| T7 | アーキテクチャ方針（責務分離・データ流れ。**技術は候補のまま確定しない**） | `10-architecture.md` ✅ |
| T8 | 戦闘仕様（行動順・ダメージ式・MP・逃走・勝敗・戻り契約） | `22-battle-spec.md` ✅ |
| T9 | ジョブ・スキル仕様（開始時ジョブ選択・数値バランス検証） | `23-job-skill-spec.md` ✅ |
| T10 | AI サポート仕様（NPC セリフ提案・承認制・Editor 専用） | `30-ai-support-spec.md` ✅ |

> 現時点で T1〜T10 の主要ドキュメントは作成済み。**フェーズ0は完了**。
> 次は「実装フェーズへ入る前の最終レビュー」を 1 回挟む。技術の最終確定は実装着手の合意時に行う。

### 🚧 T5.5 Event runtime contract 修正ゲート（フェーズ1へ進む前提条件）

> Codex レビュー（`docs/reviews/CODEX_REVIEW_001.md`）の Blocking を、フェーズ 2 着手前に潰すためのゲート。
> **このゲートを通過するまでフェーズ 1 / 2 の実装に入らない。**

- [x] GameState（flags / inventory / currentMapId / playerPosition）を `11` に定義、未定義 flag=false を明記
- [x] `startBattle` の victory / escape / defeat 結果契約を `21` に明記
- [x] 通常敵シンボル消滅を flag `enemy_slime_01_defeated` + ページ `visible:false` で表現（flag 計 3 個）
- [x] EventPage.appearance（ページ別見た目・visible 切り替え）を `11` / `21` に定義
- [x] Event の通行・当たり判定（trigger 別標準ルール）を `21` に定義
- [x] 宝箱の開封後 appearance（`chest_opened`）を `40` に反映
- [x] ポーションは戦闘中に使わない方針を `40` に明記（戦闘コマンドは attack/skill/escape）
- [x] サンプル event ID を `11`↔`40` で整合

---

## フェーズ 1: 基礎実装 — 5〜6 ターン

### 🎯 フェーズ1 の最初のスライス（最初の一歩 / Codex #002 推奨）

> **最初は「歩ける村」だけを作る。** Event Runtime / Battle Runtime / Job 選択 UI / AI / full Editor UI には広げない。

| やること |
|---|
| **固定 Project Data の読み込み（手書き JSON / 固定データ。最小エディタは作らない）** |
| `map_town` の読み込み（width/height/tileSize/layers/collision/diorama の解釈） |
| 2D タイル → 3D ジオラマ表示（固定斜め見下ろし） |
| ライト 1 灯 + 簡易影 |
| 主人公を startPosition に表示 |
| グリッド移動 + collision による通行可否 |
| カメラの主人公追従（角度固定） |

**このスライスの完了条件:** はじまりの村を、固定カメラ下で主人公が歩き回れる。

> ⚠️ 最初のスライスに **入れない**: Event Runtime / Battle Runtime / Job 選択 UI / AI Support /
> full Editor UI（タイル配置エディタ含む）/ save・load / Tauri / DB 編集 GUI /
> Event.commands の実行 / title・clear・gameover の本格 scene 遷移。

### フェーズ1 の後半（スライス完了後）

| やること |
|---|
| タイル配置の **最小エディタ**（または手書き JSON 運用を継続） |
| 表示・移動まわりの調整 |

**フェーズ1 完了条件:** はじまりの村を主人公が歩き回れる（編集 or 固定データ運用が定まる）。

---

## フェーズ 2: RPG 機能（最重要・最大の山）— 8〜10 ターン

| やること |
|---|
| **イベント実行エンジン（commands インタプリタ）** ← 心臓部 |
| メッセージウィンドウ（`message`） |
| 宝箱（`addItem` + `setFlag` + フラグ管理） |
| ワープ（`transfer`）+ 2 枚目のマップ（小さな洞窟） |
| 敵シンボル接触 → 戦闘遷移（`startBattle`） |
| ターン制戦闘（attack / skill / escape、HP/MP、勝敗） |
| アクター・ジョブ・スキル・敵データの結線 |
| ボス撃破 → `clearGame` |
| 戦闘バランス最小調整 |

**完了条件:** 村 → 洞窟 → ボス撃破 → クリアが、データ駆動で一周通る。

---

## フェーズ 3: AI サポート — 2〜3 ターン

| やること |
|---|
| NPC セリフ・クエスト文の提案（**1 機能のみ**） |
| 用途選択（村人 / 洞窟前の老人 / ボス前の警告 など） |
| セリフ案を 3 つ提示 |
| 採用ボタンを押したものだけイベントに反映（**自動反映しない**） |
| 実 AI API は後回し可（擬似 AI・テンプレート生成で可） |
| 失敗時フォールバック（手動編集） |

**完了条件:** AI 提案 → 人間承認 → イベントへ反映の流れが動く。

---

## フェーズ 4: デモ完成 — 3〜4 ターン

| やること |
|---|
| タイトル → 一周 → クリアの導線結合 |
| テストプレイ（エディタからそのまま再生） |
| ジオラマ演出の最小強化（ブルーム程度まで） |
| バグ修正・通し確認 |

**完了条件:** `01-demo01-scope.md` の成功条件 1〜9 が途切れず通る。

---

## マイルストーン

| MS | 内容 | 対応フェーズ |
|---|---|---|
| M0 | 仕様書一式が揃う | 0 |
| M1 | 村を歩ける | 1 |
| M2 | 会話・宝箱・ワープが動く | 2 前半 |
| M3 | 戦闘でボスを倒せる | 2 後半 |
| M4 | AI セリフ提案が動く | 3 |
| M5 | タイトル〜クリアが一周通る（Demo 01 完成） | 4 |

---

## リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| ジオラマ描画に時間を吸われる | RPG が遊べない箱になる | 表現はライト1灯+影で固定。霧/DOF は禁止 |
| イベントを個別実装してしまう | 工数爆発 | commands 統一を厳守（`21` 参照） |
| 敵消滅・戦闘結果をエンジン暗黙処理にする | Event.commands 統一が崩れる | flag + ページ visible / startBattle 結果契約で表現（T5.5） |
| ランタイム状態の置き場が未定義 | flag/inventory が宙に浮く | GameState を `11` に定義（T5.5） |
| ジョブ×スキルの組み合わせ爆発 | バランス調整で溶ける | ジョブ2 / スキル各2 / 敵2 を超えない |
| DB 編集 GUI に着手 | Demo 1 本分の工数 | Demo 01 は JSON 手書き / 固定データ |
| AI を多機能化 | 制御不能・遅延 | 1 機能・承認制を厳守 |

---

## 進め方のルール

- 各ターンの冒頭で「今どのフェーズ・どの MS か」を確認する
- 新要望が出たら、まず `01-demo01-scope.md` の「やらないこと」に入れられないか検討する
- フェーズ 0 が完了するまで実装に入らない
- **T5.5 ゲート（Codex 指摘の Blocking）を通過してから** フェーズ 1 / 2 に進む
- **フェーズ0 は完了**（T1〜T10 / Codex #001・#002・内部整合レビュー反映済み）。Codex #002 の判定は **GO**
- 次は **フェーズ1 の最初の狭いスライス**（下記）から着手する
